Coloca aquí el favicon del sitio, idealmente como `favicon.ico` (y opcionalmente
`favicon-32.png`, `favicon-192.png` para dispositivos móviles).

Una vez agregado, descomenta la línea correspondiente dentro de `<head>` en
index.html:

  <link rel="icon" href="assets/icons/favicon.ico">
